# Development tools. Regenerate requirements-dev.txt after editing:
#   uv pip compile requirements-dev.in --universal --python-version 3.13 -o requirements-dev.txt
-r requirements.in
pytest==9.1.1
pytest-cov==7.1.0
ruff==0.16.7
mypy==2.3.1
# FastAPI's TestClient. Already locked through langgraph; pinned here so the tests do not depend on that.
httpx==0.28.1
